<Window x:Class="Modern.View.PrezentaView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Prezenta"
        Height="700" Width="900"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Loaded="Window_Loaded">

    <Window.Resources>

        <Storyboard x:Key="FadeIn">
            <DoubleAnimation
                Storyboard.TargetName="RootBorder"
                Storyboard.TargetProperty="Opacity"
                From="0" To="1"
                Duration="0:0:0.35">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation
                Storyboard.TargetName="RootBorder"
                Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                From="30" To="0"
                Duration="0:0:0.45">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

        <Style x:Key="SlimScrollBar" TargetType="{x:Type ScrollBar}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="Transparent" Width="8">
                            <Track Name="PART_Track" IsDirectionReversed="true">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.LineUpCommand"
                                                  Style="{x:Null}" Template="{x:Null}"
                                                  Background="Transparent" IsTabStop="False"/>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border Background="{Binding DataContext.Galben, RelativeSource={RelativeSource AncestorType=Window}}"
                                                        CornerRadius="4" Margin="1"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.LineDownCommand"
                                                  Style="{x:Null}" Template="{x:Null}"
                                                  Background="Transparent" IsTabStop="False"/>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DayCard" TargetType="Button">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Width="160" Height="90"
                                CornerRadius="14"
                                Background="{Binding Gri}"
                                BorderBrush="{Binding Galben}"
                                BorderThickness="1"
                                Margin="8">
                            <Border.Effect>
                                <DropShadowEffect Color="#000000" BlurRadius="12" ShadowDepth="0" Opacity="0.3"/>
                            </Border.Effect>
                            <Grid>
                                <Border Height="3" VerticalAlignment="Top" CornerRadius="14,14,0,0">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="Transparent" Offset="0"/>
                                            <GradientStop Color="#C8A400" Offset="0.5"/>
                                            <GradientStop Color="Transparent" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                </Border>

                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding Data.Day}"
                                               Foreground="{Binding Galben}"
                                               FontSize="32"
                                               FontFamily="Georgia"
                                               FontWeight="Bold"
                                               HorizontalAlignment="Center">
                                        <TextBlock.Effect>
                                            <DropShadowEffect Color="#C8A400" BlurRadius="10" ShadowDepth="0" Opacity="0.5"/>
                                        </TextBlock.Effect>
                                    </TextBlock>
                                    <TextBlock HorizontalAlignment="Center"
                                               Foreground="{Binding Galben}"
                                               FontSize="11"
                                               FontFamily="Consolas"
                                               Opacity="0.7"
                                               Text="{Binding Data}"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{Binding Negru}"/>
                                <Setter TargetName="Bd" Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid>

        <Border Margin="10">
            <Border.Effect>
                <DropShadowEffect Color="#000000" BlurRadius="40" ShadowDepth="0" Opacity="0.7"/>
            </Border.Effect>
            <Border Background="Transparent" CornerRadius="22"/>
        </Border>

        <Border x:Name="RootBorder"
                Background="{Binding Gri}"
                BorderBrush="{Binding Galben}"
                BorderThickness="1.5"
                CornerRadius="22"
                Margin="10"
                Opacity="0">
            <Border.RenderTransform>
                <TranslateTransform/>
            </Border.RenderTransform>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="80"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- HEADER -->
                <Grid Grid.Row="0">
                    <Border Height="4" VerticalAlignment="Top" CornerRadius="22,22,0,0">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="Transparent" Offset="0"/>
                                <GradientStop Color="#C8A400" Offset="0.5"/>
                                <GradientStop Color="Transparent" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                    </Border>

                    <Grid Margin="24,0,24,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" VerticalAlignment="Center" Orientation="Horizontal">
                            <Border Width="4" Height="32" CornerRadius="2"
                                    Background="{Binding Galben}"
                                    Margin="0,0,14,0"/>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="PREZENȚĂ"
                                           Foreground="{Binding Galben}"
                                           FontFamily="Georgia"
                                           FontSize="22"
                                           FontWeight="Bold">
                                    <TextBlock.Effect>
                                        <DropShadowEffect Color="#C8A400" BlurRadius="10" ShadowDepth="0" Opacity="0.5"/>
                                    </TextBlock.Effect>
                                </TextBlock>
                                <TextBlock Text="Selectează o zi pentru detalii"
                                           Foreground="{Binding Galben}"
                                           FontFamily="Consolas"
                                           FontSize="11"
                                           Opacity="0.5"
                                           Margin="0,2,0,0"/>
                            </StackPanel>
                        </StackPanel>

                        <Button Grid.Column="1"
                                VerticalAlignment="Center"
                                BorderThickness="0"
                                Content="✕"
                                Foreground="White"
                                FontSize="13"
                                FontWeight="Bold"
                                Cursor="Hand"
                                Click="Iesire">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="CloseBd"
                                            Height="34" Width="34"
                                            CornerRadius="10"
                                            Background="{Binding Rosu}">
                                        <Border.Effect>
                                            <DropShadowEffect Color="#CC2200" BlurRadius="12" ShadowDepth="0" Opacity="0.6"/>
                                        </Border.Effect>
                                        <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="CloseBd" Property="Opacity" Value="0.8"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Grid>
                </Grid>

                <!-- BODY -->
                <Border Grid.Row="1" Margin="16,0,16,16"
                        HorizontalAlignment="Stretch">
                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled">

                        <ScrollViewer.Resources>
                            <Style TargetType="{x:Type ScrollBar}"
                   BasedOn="{StaticResource SlimScrollBar}" />
                        </ScrollViewer.Resources>

                        <ItemsControl ItemsSource="{Binding Prezente}">

                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal"
                               HorizontalAlignment="Center"
                               Margin="8"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>

                            <ItemsControl.ItemTemplate>
                                <DataTemplate>

                                    <Button Style="{StaticResource DayCard}"
                            Command="{Binding DeschidePrezentaZiCommand}"
                            CommandParameter="{Binding}">

                                        <StackPanel VerticalAlignment="Center"
                                    HorizontalAlignment="Center">

                                            <TextBlock Text="{Binding Data.Day}"
                                       Foreground="{Binding Galben}"
                                       FontSize="32"
                                       FontFamily="Georgia"
                                       FontWeight="Bold"
                                       HorizontalAlignment="Center">

                                                <TextBlock.Effect>
                                                    <DropShadowEffect Color="#C8A400"
                                                      BlurRadius="10"
                                                      ShadowDepth="0"
                                                      Opacity="0.5"/>
                                                </TextBlock.Effect>

                                            </TextBlock>

                                            <TextBlock Text="{Binding Data}"
                                       HorizontalAlignment="Center"
                                       Foreground="{Binding Galben}"
                                       FontSize="11"
                                       FontFamily="Consolas"
                                       Opacity="0.7"/>

                                        </StackPanel>

                                    </Button>

                                </DataTemplate>
                            </ItemsControl.ItemTemplate>

                        </ItemsControl>

                    </ScrollViewer>
                </Border>

            </Grid>
        </Border>
    </Grid>
</Window>