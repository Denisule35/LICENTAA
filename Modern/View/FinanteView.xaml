<Window x:Class="Modern.View.FinanteView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Finante"
        Height="600" Width="400"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True">

    <Window.Resources>
        <Style x:Key="MonthButton" TargetType="Button">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                CornerRadius="12"
                                Background="{Binding Negru}"
                                BorderBrush="{Binding Galben}"
                                BorderThickness="1"
                                Padding="16,12"
                                Margin="0,4">
                            <Border.Effect>
                                <DropShadowEffect Color="#000000" BlurRadius="8"
                                                  ShadowDepth="0" Opacity="0.3"/>
                            </Border.Effect>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding MonthName}"
                                               Foreground="{Binding Galben}"
                                               FontFamily="Consolas"
                                               FontSize="16"
                                               FontWeight="Bold"/>
                                    <TextBlock Text="{Binding Year}"
                                               Foreground="{Binding Galben}"
                                               FontFamily="Consolas"
                                               FontSize="11"
                                               Opacity="0.5"/>
                                </StackPanel>

                                <TextBlock Grid.Column="1"
                                           Text="›"
                                           Foreground="{Binding Galben}"
                                           FontSize="22"
                                           Opacity="0.6"
                                           VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="BorderThickness" Value="1.5"/>
                                <Setter TargetName="Bd" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.65"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SlimScrollBar" TargetType="{x:Type ScrollBar}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="Transparent" Width="8">
                            <Track Name="PART_Track" IsDirectionReversed="true">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.LineUpCommand"
                                                  Style="{x:Null}" Template="{x:Null}"
                                                  Background="Transparent" IsTabStop="False"/>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border Background="{Binding Galben}"
                                                        CornerRadius="4" Margin="1"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.LineDownCommand"
                                                  Style="{x:Null}" Template="{x:Null}"
                                                  Background="Transparent" IsTabStop="False"/>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Outer shadow -->
        <Border Margin="10">
            <Border.Effect>
                <DropShadowEffect Color="#000000" BlurRadius="40"
                                  ShadowDepth="0" Opacity="0.7"/>
            </Border.Effect>
            <Border Background="Transparent" CornerRadius="22"/>
        </Border>

        <Border x:Name="RootBorder"
                Background="{Binding Gri}"
                BorderBrush="{Binding Galben}"
                BorderThickness="1.5"
                CornerRadius="22"
                Margin="10">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="80"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- HEADER -->
                <Grid Grid.Row="0">
                    <!-- Top gold accent strip -->
                    <Border Height="4" VerticalAlignment="Top" CornerRadius="22,22,0,0">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="Transparent" Offset="0"/>
                                <GradientStop Color="#C8A400" Offset="0.5"/>
                                <GradientStop Color="Transparent" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                    </Border>

                    <TextBlock Text="FINANȚE"
                               Foreground="{Binding Galben}"
                               FontFamily="Georgia"
                               FontSize="28"
                               FontWeight="Bold"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center">
                        <TextBlock.Effect>
                            <DropShadowEffect Color="#C8A400" BlurRadius="18"
                                              ShadowDepth="0" Opacity="0.6"/>
                        </TextBlock.Effect>
                    </TextBlock>

                    <!-- Close button -->
                    <Button VerticalAlignment="Top"
                            HorizontalAlignment="Right"
                            Margin="0,16,16,0"
                            BorderThickness="0"
                            Content="✕"
                            Foreground="White"
                            FontSize="13"
                            FontWeight="Bold"
                            Cursor="Hand"
                            Click="Iesire">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="CloseBd"
                                        Height="34" Width="34"
                                        CornerRadius="10"
                                        Background="{Binding Rosu}">
                                    <Border.Effect>
                                        <DropShadowEffect Color="#CC2200" BlurRadius="12"
                                                          ShadowDepth="0" Opacity="0.6"/>
                                    </Border.Effect>
                                    <ContentPresenter VerticalAlignment="Center"
                                                      HorizontalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="CloseBd" Property="Opacity" Value="0.8"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>

                <!-- MONTH LIST -->
                <ScrollViewer Grid.Row="1"
                              Margin="16,0,16,16"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <ScrollViewer.Resources>
                        <Style TargetType="{x:Type ScrollBar}"
                               BasedOn="{StaticResource SlimScrollBar}"/>
                    </ScrollViewer.Resources>

                    <ItemsControl ItemsSource="{Binding Lunile}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Command="{Binding DeschidereTranzactii}"
                                        Style="{StaticResource MonthButton}"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

            </Grid>
        </Border>
    </Grid>
</Window>